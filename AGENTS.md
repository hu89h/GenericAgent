# GenericAgent 工作约定

## 测试原则

测试必须锁定真实的用户链路、外部协议、数据契约，或明确的安全、并发、限流和失败不变量。不得因为新增了一个函数、分支或参数，就临时构造一个只证明“这段代码被执行”的输入。

- 测试优先从公开入口进入，并断言用户可见结果、持久化结果或协议载荷。只有在纯算法本身就是稳定契约时，才直接测试内部函数。
- 回归测试必须有可追溯的失败来源：真实用户输入、真实第三方协议样本、已版本化 fixture，或明确记录的边界条件。测试数据不能来自某个开发者机器的绝对路径、个人目录或未版本化的隐式文件。
- 外部数据集和真实链路测试必须显式配置入口（例如环境变量），没有数据时应跳过并说明原因，不得偷偷回退到某台机器的路径。数据集结果只有在 fixture 被版本化且契约稳定时才写死精确数量；否则断言相对关系和必要不变量。
- fixture 应尽量保留导致问题的真实结构，并在 fixture 旁说明来源和测试边界。路径解析测试可以使用占位文件，但不得借此宣称完成了图片解码、MinerU 或模型链路验证；这些能力必须由对应真实协议或有效文件测试覆盖。
- Mock 只替代网络、模型、时钟、进程和其他不可控外部边界。Mock 测试必须验证边界输入如何转化为公开结果；不能只断言某个内部 mock 被调用了几次，除非调用次数本身就是限流、重试或并发契约。
- 失败注入必须模拟用户链路中确实可能发生的失败，并验证回滚、部分成功、错误分类或资源清理；不能为了覆盖异常分支而注入没有现实含义的错误。
- 不保留与当前产品契约无关的 smoke 测试、实现细节快照、硬编码开发环境测试或“先写断言再设计输入”的测试。发现这类测试时，删除或改为有来源的回归/契约测试，而不是继续堆加例子。

新增测试时，在测试名或 fixture 说明中能回答两个问题：这个输入为什么会在真实系统中出现？失败时用户或系统会观察到什么错误？回答不了时，不应新增该测试。
